# 工作约束

## 先判断访问模式

本仓库有且只有两种访问模式：

### 只读使用模式（默认）

- 用户仅提供本仓库目录、要求参考其中经验，或当前任务的交付目标位于其他项目时，一律进入只读使用模式。
- 可以读取、检索并应用本仓库中的 Skill 和经验，但不得创建、修改、删除、格式化、提交或推送本仓库中的任何内容。
- 发现经验缺失、过时或相互冲突时，只向用户提出建议；不得以“顺便完善经验库”为由修改本仓库。
- 不得把当前项目的代码、需求、路径、截图、日志或临时状态写入本仓库，包括 `.local/`。

### 经验库维护模式（必须由用户明确授权）

- 只有用户明确说明“当前会话专门用于维护、整理或演进 aicoding 经验库”时，才进入维护模式。
- “参考这个目录”“使用这里的 Skill”“把它用于当前项目”均不构成维护授权。
- 维护模式下也只能修改本仓库；实际项目仍然只读，并继续遵守下述公开内容、脱敏和质量约束。
- 每次有效更新通过检查后，默认使用中文提交并推送，无需再次询问；若检查未通过或推送失败，则保留现场并明确报告。

无法判断模式时，按只读使用模式处理并继续完成当前任务，不为确认权限而阻塞正常分析。

## 提交联动的每日整合检查

- 不设置独立的定时任务。仅在经验库维护模式下，确有有效更新准备提交时，顺带检查一次仓库是否需要整合；没有新提交就不触发。
- 检查前读取 `.local/maintenance/last-integration-check-date`。按 `Asia/Shanghai` 的自然日判断：记录为当天日期时直接跳过，否则执行检查；同一天最多检查一次。
- 检查范围包括 `AGENTS.md`、`README.md`、`notes/` 与 `skills/`，重点识别重复规则、职责重叠、相互冲突、过时结论、过度拆分和可合并的 Skill。
- 检查完成后把当天日期以 `YYYY-MM-DD` 写入上述本地文件。该记录只用于限流，必须留在 Git 忽略的 `.local/`。
- 没有明确整合价值时保持安静并继续原提交；发现可复用且能降低维护成本的机会时，只提出简洁的整合申请，不未经批准自动整合，也不阻断本次已授权内容的正常提交。

## 只读使用入口

收到本仓库目录后，按以下顺序使用：

1. 完整读取本文件，确认访问模式和边界。
2. 根据下方“仓库 Skill 路由”选择最匹配的一项；选中后完整读取对应 `SKILL.md`。
3. 只读取解决当前问题所需的 `notes/` 内容，不批量加载整个仓库。
4. 把经验作为判断框架而非项目事实；最终结论仍需以当前任务的需求、代码和验证结果为依据。
5. 没有匹配项时正常工作，不强行套用经验库内容。

## 仓库定位

- 本仓库是持续成长的 AI 编程经验库，用于沉淀可复用的方法、判断标准、反例和工作纪律。
- 内容来自真实项目观察，但公开内容必须脱离具体项目，能够被其他团队和场景复用。
- 经验允许持续修订、合并和推翻；新证据出现时，应更新原结论，而不是机械追加互相冲突的文章。

## 写入边界

- 经验库维护模式下只允许修改本仓库；其他项目只能读取、检索和分析。
- Git 跟踪区只允许保存通用经验，不得保存任何可识别具体项目的信息。
- 项目观察状态、来源路径、临时摘录和验证记录统一放在 `.local/`，该目录必须保持 Git 忽略。
- 不得为了方便引用而复制实际项目文件、代码、截图、日志、数据、需求原文或内部链接到本仓库。

## 禁止进入 Git 的内容

- 公司、客户、项目、仓库、分支、工单或人员的可识别名称；
- 本机绝对路径、内网地址、私有仓库地址和环境连接信息；
- 业务原始数据、表结构原文、日志、截图、附件和需求原件；
- 访问令牌、账号、密钥、Cookie、数据库连接或其他凭据；
- 尚未完成抽象、仍只能在单一项目语境中理解的观察记录。

## 经验进入公开区的流程

1. 在 `.local/observations/` 记录事实、来源和当前状态。
2. 区分事实、推断、建议和未验证假设。
3. 去除项目名称、路径、数据和专有术语，提炼可迁移的根因与判断方法。
4. 用通用示例重写，确保不了解原项目的人也能独立理解。
5. 合并到现有主题；只有形成独立知识单元时才新增文档。
6. 提交前运行 `scripts/check-public-content.sh`，并人工检查脱敏和可复用性。

## Skill 沉淀提醒

- 当对话中形成了经过验证、可跨项目复用且会改变后续 AI 工作方式的方法时，读取并使用 `skills/aicoding-skill-curator/SKILL.md` 判断是否值得沉淀。
- 先完成当前任务，再在自然收口点简短询问；未经用户同意，不自动创建或修改 Skill。
- 一次性项目事实、临时猜想和普通经验文章不触发 Skill 提醒。

## 仓库 Skill 路由

- 面对陌生技术方案、无法独立核验 AI 结论或担心 AI 自由发挥时，使用 `skills/bridge-ai-knowledge-gaps/SKILL.md`，把结论转成有证据、可理解、可逐步验证的决策。
- 功能主体完成后进入本地联调、需要保持前后端随时可用并连续修复缺陷时，使用 `skills/run-local-debug-loop/SKILL.md`，建立运行基线、可用性检查、缺陷队列和“修复后由用户验证”的闭环。
- 页面较多、角色较多或需要逐页记录人工验收进度时，使用 `skills/track-manual-ui-acceptance/SKILL.md`，从当前代码盘点页面并生成由易到难、带直达入口和问题编号的检查清单。
- 接手陌生项目并被要求尽快启动、编译或进入首个可观察页面时，使用 `skills/start-unfamiliar-project-fast/SKILL.md`，先做环境预检和批量诊断，再用最低成本探针逐级验证。
- 评审国内企业审批、OA/BPM 的需求或原型，尤其需要识别概念混维、低代码痕迹和无真实场景的平台功能时，使用 `skills/review-enterprise-workflow-design/SKILL.md`。
- 创建、评审、重组或持续演进需求与软件设计文档时，使用 `skills/evolve-design-docs/SKILL.md`，遵循人类阅读优先、ER 建模优先、Mermaid 按问题选型和正文去噪的文档习惯。
- 发散讨论后需要清理需求或设计文档、消除旧方案残留并形成开发基线时，使用 `skills/converge-requirement-docs/SKILL.md`。
- 收到其他评审者或模型的意见，需要将其作为参考并保留独立判断时，使用 `skills/evaluate-external-review/SKILL.md`。
- 维护模式下发现可跨项目复用、已经验证且会改变后续工作方式的方法时，使用 `skills/aicoding-skill-curator/SKILL.md` 判断是否值得沉淀；只读模式不得因此修改本仓库。

## 内容质量

- 文档首先是给人阅读和决策的，不是保存 AI 推理过程或工作流水的容器。
- 主文档只保留当前有效结论、必要依据和可执行规则；往返讨论、被推翻方案、临时假设和重复引用属于噪音，应删除或移入本地观察区。
- 先归纳后展开：每个章节先说明结论和整体关系，再补必要细节，避免让读者从大量文字中自行提炼重点。
- 优先使用 Mermaid 表达业务对象关系、处理流程、状态转换、调用时序和模块依赖；能够用一张图讲清的内容，不用多段文字重复描述。
- Mermaid 图必须服务于理解，图后只补充图中无法表达的约束和结论，不把同一信息再完整复制成表格与长文。
- 优先记录“如何判断”和“为什么”，避免只记录一次性的解决步骤。
- 一个结论应包含适用条件、反例或失效边界，避免把局部经验写成普遍真理。
- 不把 AI 扩写出的合理猜测冒充事实或已确认需求。
- 文档署名统一使用“范显”，不使用 AI 或工具名称署名。
