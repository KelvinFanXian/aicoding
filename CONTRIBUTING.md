# 内容纪律

## 可以提交

- 跨项目可复用的判断方法、设计原则和工作流程；
- 已去除项目背景的反例与失败模式；
- 对既有经验的修正、适用边界和验证方法；
- 用虚构名称和最小数据构造的通用示例。

## 不可以提交

- 实际项目的名称、路径、仓库地址、分支、工单和人员信息；
- 需求原文、设计原稿、代码片段、数据库结构、日志、截图和业务数据；
- 只对某个项目有效的当前状态、进度和待办；
- 凭据、内网信息及任何不适合公开的内容。

## 本地观察记录

需要保留真实来源和上下文时，写入：

```text
.local/observations/   当前观察对象、事实和疑问
.local/sources/        必要的本地来源索引，不复制原件
.local/sessions/       会话进度和下一步
```

`.local/` 整体被 `.gitignore` 排除。项目原件应留在原项目中，本仓库只保存路径索引和观察笔记。

## 表达规范

- **人读优先**：正文面向需要理解、评审和实施的人，不展示生成过程和完整思维链。
- **结论优先**：章节开头先给归纳结论，再展开规则、边界和例外。
- **主线唯一**：主文档只描述当前有效口径；旧方案、争论过程和临时假设不与正式结论并列。
- **图胜于堆字**：对象关系、业务流程、状态机、调用时序和模块依赖优先使用 Mermaid。
- **避免重复**：图、表格和文字各承担一种职责，不以三种形式重复同一批信息。
- **控制密度**：表格用于精确对照，列表用于短规则，文字用于解释原因；没有信息增量的内容直接删除。

推荐选择：

| 要表达的内容 | 优先形式 |
| --- | --- |
| 业务对象和关系 | Mermaid ER 图 |
| 操作步骤、审批和生效路径 | Mermaid 流程图 |
| 状态变化 | Mermaid 状态图 |
| 服务调用与事件顺序 | Mermaid 时序图 |
| 字段、规则或方案对照 | 表格 |
| 结论、原因和适用边界 | 简短文字 |

## 发布前自检

1. 删除公司、客户、项目、仓库和人员的可识别信息。
2. 删除本机路径、内部链接、真实编号、数据和代码原文。
3. 确认文章离开原项目上下文仍可理解。
4. 标明结论的适用条件和已知边界。
5. 删除生成过程、往返讨论、重复解释和已失效口径。
6. 检查可否用一张 Mermaid 图替代大段关系或流程描述。
7. 运行：

   ```bash
   ./scripts/check-public-content.sh
   ```

8. 查看 `git diff --cached`，逐项确认没有误纳本地材料。
